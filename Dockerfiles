# 1. Use an official lightweight Python image
FROM python:3.10-slim

# 2. Prevent Python from buffering stdout/stderr (better logging)
ENV PYTHONUNBUFFERED=1

# 3. Install system dependencies (Poppler for PDF handling)
RUN apt-get update && apt-get install -y \
    poppler-utils \
    && rm -rf /var/lib/apt/lists/*

# 4. Set the working directory container
WORKDIR /app

# 5. Copy requirements and install Python dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 6. Copy the rest of the application code
COPY . .

# 7. Start the application
# We use 'sh -c' to ensure the $PORT environment variable is read correctly
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]